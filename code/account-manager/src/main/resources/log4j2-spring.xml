<?xml version="1.0" encoding="UTF-8"?>
<Configuration name="account-manager-application"
               xmlns="http://logging.apache.org/log4j/2.0/config"
               status="error">

    <Properties>
        <Property name="CONSOLE_PATTERN">%X{TRACE_ID} %date{DEFAULT} [%-5level] (%file:%line) - %msg%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${CONSOLE_PATTERN}"/>
        </Console>
        <File name="ErrFile" fileName="logs/error.log">
            <PatternLayout pattern="${CONSOLE_PATTERN}"/>
        </File>
    </Appenders>

    <Loggers>
        <Logger name="org" level="warn" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="com" level="warn" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Root level="info">
            <AppenderRef ref="Console"/>
        </Root>
        <Root level="error">
            <AppenderRef ref="ErrFile"/>
        </Root>
    </Loggers>

</Configuration>